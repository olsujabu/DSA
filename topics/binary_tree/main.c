// author : Buyandelger.J
// title :  Binary_Tree
#include "binary_tree.h"
#include <stdio.h>
#include <stdlib.h>

int main()
{
  Tree *tree = malloc(sizeof(Tree));
  tree->nodes = 0;
  free(tree);
  return 0;
}